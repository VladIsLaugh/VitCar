{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pnpm --filter=@vitauto/api exec prisma migrate resolve --rolled-back 20260512000000_add_auth_models 2>/dev/null || true && pnpm --filter=@vitauto/api migrate:prod && [ \"$APP_ENV\" != \"production\" ] && pnpm --filter=@vitauto/api db:seed; node apps/api/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
